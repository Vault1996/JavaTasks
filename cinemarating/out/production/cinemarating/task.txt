- 1) Mail, подтверждение почты;
+ 2) Validation, отдельными классами в Command;
+ 3) MessageManager, сообщения из программы;
(или булевая переменная, которая проверяется внутри программы и уже интернализация из jsp)
+ 4) Регистрация;
+ 5) Почитать Memento; Если что, хранить его в Session;
+ 6) Все параметры, которые достаются из форм заносятся в константы, хоть и используются один раз;
7) Защита от F5 (возможный вариант: записывать последнюю команду в строку и проводить проверку);
+ 8) LOGOUT (request.getSession().invalidate(); return page;);
+ 9) Service не обязательно связывать с командой. Например, USER_SERVICE)
+ 10) Session: роль, имя, локаль (последняя команда);
+ 11) Написать фильтры; В фильтрах указать локаль по умолчанию;
+ 12) Выбор языка на каждой почти странице;
+ 13) .properties файл для имён страниц;
14) LOGGER везде и локализация;
private static final Logger LOGGER = LogManager.getLogger(класс.class);
+ 15) Тесты;
+ 16) Пользовательские теги .tld;
+ 18) Поменять переменные в локализации на boolean и уже локализация из jsp
19) Validation EditProfile AddMovie EditMovie
20) Добавление картинок с именем id.
+ 21) i18n